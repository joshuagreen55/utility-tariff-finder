# Upper bounds on the DB / task stack keep fresh installs reproducible.
# SQLAlchemy 2.1 maps a bare postgresql:// URL to psycopg (v3), which is not
# installed, so `alembic upgrade head` and every sync engine broke on a
# fresh `pip install` (audit F9). Bump these deliberately, with a migration
# run in CI, never implicitly.
fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36,<2.1
asyncpg>=0.30.0,<1
alembic>=1.14.0,<2
geoalchemy2>=0.15.0,<1
shapely>=2.0.6
geopandas>=1.0.1
httpx>=0.28.0
PyJWT[crypto]>=2.10.0
google-auth>=2.36.0
pydantic>=2.10.0,<3
pydantic-settings>=2.7.0,<3
python-dotenv>=1.0.1
celery[redis]>=5.4.0,<6
redis>=5.2.0
pdfplumber>=0.11.0
playwright>=1.49.0
pytesseract>=0.3.13
pdf2image>=1.17.0
Pillow>=11.0.0
beautifulsoup4>=4.12.3
lxml>=5.3.0
openpyxl>=3.1.5
pandas>=2.2.3
pyproj>=3.6.0
requests>=2.32.0
psycopg2-binary>=2.9.9,<3
anthropic>=0.40.0
google-genai>=1.0.0
